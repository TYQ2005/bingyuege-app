# 冰阅 APP v1.0.0 APK 发布说明

## 快速发布

如果您想立即发布 v1.0.0 版本的 APK 到 GitHub Releases，请执行:

```bash
# 完整的自动化发布流程
chmod +x publish-release.sh
./publish-release.sh v1.0.0
```

这将自动:
1. ✅ 验证版本号和 Git 状态
2. ✅ 选择构建方式（本地/Docker/GitHub Actions）
3. ✅ 构建 Release APK
4. ✅ 推送到 GitHub
5. ✅ 创建 GitHub Release
6. ✅ 上传 APK 文件

## 📋 详细发布选项

### 选项 1: 使用 GitHub Actions（推荐 ⭐）

**优点:**
- 无需本地安装 Java、Android SDK
- 在云端自动构建
- 构建环境一致
- CI/CD 集成

**步骤:**
```bash
git push origin v1.0.0
```

然后访问: https://github.com/TYQ2005/bingyuege-app/actions

等待 GitHub Actions 完成构建，然后使用 github-release.sh 创建发布。

### 选项 2: 本地构建

**前置要求:**
- ✅ Node.js 14+
- ✅ npm 6+
- ✅ Java JDK 11+
- ✅ Android SDK
- ✅ Cordova CLI

**步骤:**
```bash
# 安装依赖
npm install

# 添加 Android 平台
cordova platform add android@latest

# 构建 Release APK
npm run build
# 或
cordova build android --release

# 创建 GitHub Release
export GITHUB_TOKEN=your_token_here
./github-release.sh v1.0.0
```

### 选项 3: Docker 构建

**优点:**
- 无需本地配置复杂的 Java/Android SDK
- Docker 容器提供隔离的编译环境
- 跨平台兼容

**前置要求:**
- ✅ Docker

**步骤:**
```bash
chmod +x docker-build.sh
./docker-build.sh

# 创建 GitHub Release
export GITHUB_TOKEN=your_token_here
./github-release.sh v1.0.0
```

## 🔑 GitHub Token 设置

要上传 APK 到 GitHub Releases，需要设置 GitHub 令牌:

1. **生成 Token**
   - 访问: https://github.com/settings/tokens/new
   - 选择权限: `repo` (完整的代码库访问权限)
   - 点击 "Generate token"
   - 复制令牌

2. **设置环境变量**
   ```bash
   export GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

3. **验证**
   ```bash
   curl -H "Authorization: token $GITHUB_TOKEN" https://api.github.com/user
   ```

## 📦 发布文件配置

支持的 APK 文件:

| 文件类型 | 位置 | 说明 |
|---------|------|------|
| Release（无签名） | `platforms/android/app/build/outputs/apk/release/app-release-unsigned.apk` | Google Play 发布前需签名 |
| Release（已签名） | `platforms/android/app/build/outputs/apk/release/app-release.apk` | 推荐用于 GitHub Releases |
| Debug | `platforms/android/app/build/outputs/apk/debug/app-debug.apk` | 仅用于测试 |

## 🔐 APK 签署（可选）

如果构建出的是无签名 APK，需要签署:

### 生成签名密钥（首次）
```bash
keytool -genkey -v -keystore bingyuege-release-key.jks \
    -keyalg RSA -keysize 2048 -validity 10000 \
    -alias bingyuege \
    -storepass password123 \
    -keypass password123
```

### 签署已有的 APK
```bash
jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 \
    -keystore bingyuege-release-key.jks \
    -storepass password123 \
    platforms/android/app/build/outputs/apk/release/app-release-unsigned.apk \
    bingyuege

# 优化
zipalign -v 4 platforms/android/app/build/outputs/apk/release/app-release-unsigned.apk \
    platforms/android/app/build/outputs/apk/release/app-release.apk
```

## ✅ 发布检查清单

发布前，请检查:

- [ ] 版本号正确（v1.0.0）
- [ ] 所有代码已提交
- [ ] 版本标签已创建
- [ ] APK 文件已构建
- [ ] APK 大小合理（10-30MB）
- [ ] GitHub Token 已设置
- [ ] 发布说明已准备
- [ ] 更新日志已更新

## 🔄 工作流程图

```
┌─────────────────────────────────────────────────────┐
│ 1. 验证版本和 Git 状态                              │
└─────────────────────────────────────────────────────┘
                          ↓
                 ┌─────────┴─────────┐
                 ↓                   ↓
          ┌──────────────┐    ┌───────────┐
          │ 本地构建     │    │ Docker    │
          │ (需要SDK)    │    │ 构建      │
          │              │    │           │
          └──────────────┘    └───────────┘
                 │                   │
                 └─────────┬─────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│ 3. 推送到 GitHub (git push)                         │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│ 4. GitHub Actions 自动构建（如使用）               │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│ 5. 创建 GitHub Release                             │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│ 6. 上传 APK 文件                                   │
└─────────────────────────────────────────────────────┘
                          ↓
                    ✅ 发布完成
```

## 📱 用户获取 APK

发布完成后，用户可以:

1. 访问: https://github.com/TYQ2005/bingyuege-app/releases
2. 找到 v1.0.0 版本
3. 下载 APK 文件
4. 在 Android 设备上安装

## 🐛 故障排除

### GitHub Actions 构建失败
- **原因**: 依赖下载失败或版本不兼容
- **解决**: 检查网络连接，查看 Actions 日志

### APK 签署失败
- **原因**: 密钥库密码错误或文件损坏
- **解决**: 重新生成密钥库

### 网络问题
- **原因**: npm registry 或 GitHub API 连接不稳定
- **解决**: 使用镜像源，重试命令

## 📞 联系支持

如有问题，请:
1. 查看 BUILD_GUIDE.md
2. 检查 GitHub Issues: https://github.com/TYQ2005/bingyuege-app/issues
3. 查阅 README.md

## 🎉 完成！

现在您可以通过以下任何一种方式发布 APK:

```bash
# 最简单的方式：完整自动化流程
./publish-release.sh v1.0.0

# 或分步执行：
npm run build
export GITHUB_TOKEN=your_token
./github-release.sh v1.0.0
```

---

**版本**: 1.0.0  
**最后更新**: 2026-02-13  
**维护者**: 冰阅开发团队
